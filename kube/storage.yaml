# 디스크 공간을 동적으로 할당받기 위한 PVC(Persistent Volume Claim) 리소스입니다.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gigachad-pvc
spec:
  # 단일 노드에서 읽기/쓰기가 가능하도록 접근 모드를 설정합니다.
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # 100MB의 물리적 스토리지 공간을 요청합니다.
      storage: 100Mi
